package com.shipmonk.testingday;

public class FixerClientTest {

    // mock the fixer service and test the client
    // we don't need the whole application context so initialize only the client

}
